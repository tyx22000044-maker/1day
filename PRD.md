# 1Day — Product Requirements Document

**版本** v3.0 · **日期** 2026-06-29 · **作者** 谈昀轩
**状态** 规划中 · **产品家族** 1App Family

---

## 一、产品背景

### 1.1 家族定位

1App Family 已有三个核心方向：

- **1Cash**：记录**钱**的流动——支出、收入、账户、预算、退款、报销和报告。
- **1Life**：记录**身体**与生活状态——饮食、营养、饮水、运动、习惯、状态日志和 Apple Health。
- **1Track**：记录**在线订阅服务**——Netflix、Spotify、iCloud+ 等网络订阅的价格、周期、续费提醒。

这三个 App 覆盖了**「已经发生的」和「正在持续的」**，但缺少一个**计划层**——管理未来要做的事情、记录日程中的思考、保存重要信息：

- 今天要做什么？
- 早上 10 点的会议聊了什么、我有什么想法？
- 下周有什么 deadline？
- 这个想法还没整理？
- 这周完成了什么，什么被拖延了？

**1Day 是 1App Family 的第四个 App，负责「计划、日程、笔记、和复盘」。**

### 1.2 家族全景

```
              1App Family — 记录与规划

  过去/持续                          未来/思考
┌─────────────────────┐    ┌──────────────────────────┐
│ 1Cash  「钱」       │    │ 1Day  「事 + 思」       │
│ 支出/收入/预算/报销  │    │ 任务/日程/笔记/想法/复盘  │
├─────────────────────┤    │                          │
│ 1Life  「身体」     │    │                          │
│ 饮食/运动/习惯/日志  │    │                          │
├─────────────────────┤    │                          │
│ 1Track 「在线订阅」 │    │                          │
│ 网络服务/续费/价格   │    │                          │
└─────────────────────┘    └──────────────────────────┘
```

**边界明确：**

- 1Track 只管在线订阅服务（Netflix、Spotify 等），1Day 管所有"要做的事"和"要记的想法"。二者不重叠。
- 1Cash 记录金额和账户流动，1Day 记录任务是否完成和过程中的思考。
- 1Life 记录身体和生活数据，1Day 管理计划和待办。
- **前期不做数据联动，各自闭环。**

### 1.3 一句话定位

> **安排要做的，记录在想的，复盘已完成的。**

### 1.4 为什么是 1Day

市面上不缺 todo App，但 1Day 的独特价值在于：

**① 不只是任务，还有思考**
大多数 todo App 止步于"勾选完成"。1Day 让用户在日程下记录会议纪要、个人思考、灵感想法，所有笔记可搜索、可回溯。不是任务管理器，而是个人事务 + 思考的中心。

**② 家族生态补位**
1Day 是 1App Family 里管"未来的事"和"脑中的想法"的那一块。四个 App 组成完整的个人生活管理系统。

**③ AI 原生 + 用户掌控**
AI 从 V1.0 就是内置的交互方式。用户自选服务商、自持 API Key，不被锁定。

**④ 计划 → 执行 → 复盘的闭环**
大多数 todo App 只关注"接下来做什么"，1Day 同时关注"做了什么、想了什么、做得怎么样"。

**竞品对比：**

| | Apple Reminders | Things 3 | 滴答清单 | **1Day** |
|---|---|---|---|---|
| 本地优先 | ✅ | ✅ | ❌ 需账号 | ✅ |
| AI 辅助 | ❌ | ❌ | 有但弱 | ✅ 6 家服务商 |
| 日程笔记 | ❌ | ❌ | ❌ | ✅ 事项下挂载笔记 |
| 想法收集 | ❌ | ✅ 收件箱 | ❌ | ✅ 笔记 Tab |
| 复盘/回顾 | ❌ | ✅ Logbook（浅） | ❌ | ✅ AI 复盘 |
| 证件管理 | ❌ | ❌ | ❌ | ✅ 照片存储+快速取用 |
| 生态联动 | Apple 生态 | 独立 | 独立 | 1App Family |
| 隐私控制 | Apple 托管 | 本地 | 服务商托管 | 用户持有 Key |
| 价格 | 免费 | ¥388 买断 | 订阅制 | 免费 + 自付 AI |

### 1.5 面向用户的表达

> 1Day 是一个本地优先的 AI 生活计划工具。快速记下要做的事，在日程下写会议纪要和个人想法，随时搜索你记过的一切，定期回顾完成情况。

### 1.6 产品原则（家族统一）

- **本地优先**：数据默认保存在设备上。
- **AI 可选**：不配置 AI 也能使用核心功能。
- **用户确认**：AI 只生成草稿，关键记录由用户确认后保存。
- **聚焦场景**：每个 App 只解决一个明确的问题。
- **数据可导出**：支持备份，用户掌握自己的数据。

---

## 二、目标用户

### Persona A · 小陈 — 事情太多记不住（核心用户，占比 50%）

**年龄**：25-32 岁，上班族
**特征**：每天有大量琐事和会议，经常忘记要做的事情
**核心痛点**：脑子里事情很多但没地方放，会议纪要散落在各处，备忘录太乱
**关键动机**：快速抓取想法、不漏掉重要事项、会后随手记一笔
**使用路径**：Onboarding → 快速输入任务 → 开会时记笔记 → 收到提醒 → 完成勾掉
**留存关键**：通知提醒把用户拉回来，今日视图给用户掌控感，笔记让信息不丢失

### Persona B · 老黄 — 需要规划和复盘（深度用户，占比 35%）

**年龄**：30-40 岁，管理者/自由职业者
**特征**：日程密集，需要规划未来几周，会议多且需要记录决策
**核心痛点**：无法在一处看到所有 upcoming 事项，会议记录分散，缺少复盘习惯
**关键动机**：清晰的日程规划 + 会议记录归档 + 定期的自我复盘
**使用路径**：Onboarding → 批量创建任务 → 会议后写纪要 → 每周在笔记 Tab 回顾 → AI 复盘
**留存关键**：日程+笔记的结合让 App 成为每日必用工具

### Persona C · 小王 — 想法多但执行力弱（潜在用户，占比 15%）

**年龄**：22-28 岁，学生/初级职场人
**特征**：有很多想做但没去做的事情，想法零散
**核心痛点**：想法没有地方整理和追踪
**关键动机**：随手记想法 → 整理 → 变成可执行的任务
**使用路径**：下载 → 在笔记 Tab 随手记 → 偶尔整理转为任务 → 逐步养成习惯
**留存关键**：笔记 Tab 的低门槛捕捉体验

---

## 三、版本路线图

### 总览

| 版本 | 主题 | 核心内容 | 用户价值 |
|------|------|----------|----------|
| **V1.0** | 核心计划 + 笔记 | 任务 CRUD + 今日视图 + 快速输入 + 基础笔记 + 基础通知 + AI（create_task）+ Onboarding + 设置 + JSON 备份 | 能记事、能记想法、能提醒、能用 AI 建任务 |
| **V1.1** | 组织能力 | 收藏集 + 标签 + 筛选排序 + 全局搜索（任务+笔记）+ 子任务 | 内容多了以后能分组管理和检索 |
| **V1.2** | 日程与日历 | 事件类型 + 日历视图 + 时间块 + 笔记挂载到事件 + 重复任务 | 能管日程、能在会议下记纪要 |
| **V1.3** | 想法、证件与复盘 | 想法模型 + 想法转任务 + 证件管理（设置内）+ 复盘笔记 + AI 复盘 | 想法闭环 + 证件快速取用 + 回顾闭环 |
| **V2.0+** | 家族联动 | 与 1Cash/1Life/1Track 数据互通 | 跨 App 的统一生活管理 |

### V1.0 详细范围

**核心功能**：
- 任务创建（标题 + 可选备注 + 可选日期 + 可选优先级）
- 任务编辑、删除、标记完成
- 今日视图（过期置顶 + 今日任务 + 已完成折叠 + 底部进度摘要）
- 快速输入栏（今日视图顶部常驻）
- 计划列表（所有任务按日期分组：已过期 / 今天 / 明天 / 本周 / 更晚 / 未安排）
- 基础笔记（创建、编辑、删除独立笔记，列表浏览）
- 基础本地通知（到期当天提醒）

**AI 功能**：
- AI 聊天（家族统一骨架）
- `create_task` 意图：自然语言 → 任务草稿 → 确认页 → 保存

**基础设施**：
- Onboarding（家族骨架 + 默认提醒时间设定）
- 设置页（家族统一结构）
- JSON 完整备份/恢复
- 空状态引导（每个 Tab）

**不包含**：
- 收藏集、标签、子任务、全局搜索（→ V1.1）
- 事件类型、日历视图、时间块、笔记挂载事件、重复任务（→ V1.2）
- 想法模型、证件管理、复盘（→ V1.3）

### V1.1 详细范围

- 收藏集（Collection）：创建、编辑、删除、任务归属、进度显示
- 标签（Tag）：创建、编辑、删除、按标签筛选
- 子任务：任务内可添加子任务，可折叠
- 筛选：按状态（待办/已完成）、按标签、按日期范围
- 排序：按日期、按优先级、按创建时间、手动拖拽
- **全局搜索**：跨任务和笔记的标题 + 内容全文搜索
- Tab 2 增加「收藏集」分段视图
- 笔记支持标签分类

### V1.2 详细范围

- **PlanItem 新增 type**：task（任务）/ event（事件/日程）
- 事件：有 startDate + endDate（时间段），用于表示会议、行程等
- **笔记挂载到事件**：一个事件下可以有多条笔记（会议纪要、个人思考），笔记同时出现在 Tab 4 笔记列表中
- 日历视图：月视图 / 周视图 / 日视图切换
- 时间块：事件在日历上显示为色块
- 重复任务/事件：每天 / 每周 / 每月 / 每年 / 工作日
- AI 增强：`create_event`（创建日程）、`create_tasks`（一句话多任务）、`get_today_tasks`（上下文注入）、`get_upcoming`（近期事项注入）

### V1.3 详细范围

- **Idea 独立模型**：极简捕捉（标题+内容），支持转为任务
- 想法在 Tab 4 笔记 Tab 中作为一个分类存在
- **证件管理**（设置内）：证件照片存储、到期提醒、Face ID 保护、快速分享/下载
- **复盘**：日/周复盘笔记 + AI 生成复盘摘要
- AI 增强：`get_review_summary`（复盘摘要）、`organize_notes`（AI 建议笔记整理）
- ReviewEntry 模型

---

## 四、导航结构

### 4.1 五 Tab 结构（家族统一）

```
Tab 1: 今天        ← 今日待办 + 快速输入 + 进度摘要（日常入口）
Tab 2: 计划        ← 所有任务/事件 + 收藏集（组织管理）
Tab 3: 🤖 AI      ← 家族统一 AI 中心
Tab 4: 笔记        ← 所有笔记/想法（思考中心，可搜索）
Tab 5: 设置        ← 家族统一设置 + 证件管理
```

**Tab 排列规则（家族统一）**：
- AI 永远在中央（Tab 3）
- 设置永远是最后（Tab 5）
- AI Tab 视觉处理（中央按钮突出 + 触觉反馈）与家族一致

### 4.2 Tab 1 — 今天

用户每天打开 App 的第一个画面，一眼看清今天要做什么。

**页面结构**（自上而下）：
1. **日期标题**：「6月29日 周日」
2. **快速输入栏**：占位文字「记下一件事…」，回车默认创建今天的任务
3. **过期未完成**（红色标记，置顶）：显示所有已过期但未完成的任务
4. **今日任务/事件**：按时间 → 优先级排列
5. **今日已完成**：折叠区域，灰显，点击展开
6. **今日进度卡片**（底部，折叠式）：今日完成 X/Y · 本周完成 N · 连续 D 天

**交互细节**：
- 快速输入：回车 → 创建今日任务；长按发送 → 弹出菜单（今天 / 明天 / 稍后安排；「本周」和「选择日期」未实现）
- 长按任务：设定日期 / 标记完成 / 删除（列表不是 `List`，没有行级左右滑；详情页提供同等入口）
- 点击任务：进入详情编辑
- 点击事件（V1.2）：进入事件详情，可查看/添加笔记
- 空状态：「今天没有待办事项」+ 引导添加
- 进度卡片点击展开：近 7 天完成趋势迷你图

### 4.3 Tab 2 — 计划

所有任务和事件的管理中心。

**页面结构**（自上而下）：
1. **分段切换**：「列表」/「收藏集」（V1.1 加入收藏集模式）
2. **列表模式**：任务按日期分组
   - 未安排（无日期任务）
   - 已过期
   - 今天
   - 明天
   - 本周
   - 更晚
3. **收藏集模式**（V1.1）：收藏集网格，每个显示名称 + 图标 + 进度
4. **右上角**：筛选/排序按钮

**关键决策**：
- "未安排"分组置顶显示，让用户看到还有哪些事没安排日期
- 用户在这里给无日期任务分配日期 → 它出现在 Tab 1 对应日期的视图里
- 不引入独立的"收件箱"概念，减少认知负担
- V1.2 起，事件（有 start/end 时间的日程）也在此列表中显示，以时间色块区分

### 4.4 Tab 3 — AI（家族统一）

与 1Cash/1Life/1Track 保持一致的 AI 模块。详见第六节。

### 4.5 Tab 4 — 笔记

所有笔记、想法、会议纪要的统一管理中心。核心价值：**一个地方看到你记过的一切，全部可搜索。**

**V1.0 内容**：
1. **顶部搜索栏**：标题 + 内容全文搜索（V1.0 基础搜索，V1.1 升级为全局搜索）
2. **快速新建按钮**：一键创建新笔记
3. **笔记列表**：按更新时间倒序，显示标题 + 内容预览 + 时间
4. **空状态**：「随手记下你的想法」+ 引导创建

**V1.2 升级**（事件笔记）：
- 笔记列表中区分：「独立笔记」和「事件笔记」（显示关联的事件名称）
- 例如：一条标题为「Q3 规划会 - 会议纪要」的笔记，下方显示「关联：Q3 规划会议 · 6/30 10:00-12:00」
- 从事件详情页也可以进入笔记编辑
- **同一条笔记**在事件详情和笔记 Tab 都能看到，不是两份数据

**V1.3 升级**（想法 + 复盘）：
- 分段切换：「全部」/「笔记」/「想法」/「复盘」
- 想法：极简卡片列表，支持一键转为任务
- 复盘笔记：日/周切换，显示该时段完成统计 + 用户记录 + AI 摘要

### 4.6 Tab 5 — 设置（家族统一）

分组列表（`.insetGrouped`），与 1Cash 设置页一致结构。

从上到下：
1. 个人资料（头像 + 昵称，家族统一 `UserAvatarView`）
2. 基础设置（默认提醒时间、默认视图、语言、外观）
3. 收藏集管理（V1.1）
4. **证件管理**（V1.3）
5. AI 配置（6 家服务商选择、Key 管理、测试连接）
6. 数据管理（导出/导入 JSON 备份、清空数据）
7. 关于（用户手册、隐私说明、版本号）

---

## 五、核心功能规格

### 5.1 笔记系统

**定位**：1Day 的笔记不是一个独立的笔记 App（不是 Apple Notes 的替代品），而是围绕「事」的思考记录。用户在这里记录的是：

- 会议聊了什么、自己的想法（关联到日程事件）
- 一个突然冒出的灵感（独立笔记）
- 对某件任务的思考过程（关联到任务）
- 每周的复盘总结（复盘笔记）

**V1.0 笔记功能**：
- 创建独立笔记（标题 + 正文纯文本）
- 编辑、删除笔记
- 笔记列表按更新时间排序
- 基础搜索（标题 + 内容）

**V1.2 事件笔记**：
- 从事件详情页点击「添加笔记」→ 创建一条关联到该事件的笔记
- 一个事件下可以有多条笔记（会议纪要一条、个人思考一条）
- 事件笔记在 Tab 4 笔记列表中统一展示，带有关联事件标签
- 搜索覆盖所有笔记（独立笔记 + 事件笔记）

**笔记不做的事**：
- ❌ 不做富文本编辑（加粗、插图、表格）——纯文本，保持轻量
- ❌ 不做笔记分享/协作
- ❌ 不做笔记文件夹层级——用标签（V1.1）和搜索来组织

### 5.2 证件管理（设置内，V1.3）

**定位**：用户经常需要身份证、护照等证件照片（办签证上传、酒店入住登记等），但找照片很麻烦。1Day 在设置里提供一个安全的证件照片存储区。

**功能**：
- 添加证件：名称 + 证件类型 + 正反面照片 + 到期日期（可选）
- 预设证件类型：身份证、护照、驾照、港澳通行证、社保卡、其他
- 证件列表：网格展示，每张证件显示类型图标 + 名称 + 到期状态
- 查看证件：点击查看大图，支持缩放
- **快速下载**：长按或点击「保存到相册」，一键保存照片到 Camera Roll
- **快速分享**：系统分享 Sheet，可直接发送到微信/邮件等
- 到期提醒：如果设置了到期日期，到期前 30 天发本地通知
- **安全保护**：进入证件管理需要 Face ID / Touch ID 验证
- 照片存储在 App 沙盒内，不上传到任何服务器

**技术要点**：
- 照片用文件系统存储（非 SwiftData，避免大二进制数据影响性能）
- SwiftData 只存元数据（名称、类型、到期日、照片文件路径）
- JSON 备份中包含证件元数据，不包含照片（照片需单独备份或 iCloud 同步时处理）

### 5.3 事件/日程（V1.2）

**定位**：V1.2 起 PlanItem 增加 `event` 类型，用于表示有时间段的日程（会议、行程等）。

**与任务的区别**：
- 任务（task）：有一个截止日期，核心动作是"完成"
- 事件（event）：有一个开始时间和结束时间，核心动作是"参加"并"记录"

**事件功能**：
- 创建事件：标题 + 开始时间 + 结束时间 + 地点（可选）+ 备注
- 事件详情页底部：「笔记」区域，显示关联笔记列表 + 「添加笔记」按钮
- 日历视图中事件显示为时间色块
- 事件完成后不"勾选"，而是自动按时间归入"已过去"

**典型用例**：
1. 用户创建事件「Q3 规划会 · 10:00-12:00」
2. 会议进行中或会后，在事件详情页点击「添加笔记」
3. 记录会议纪要：讨论了什么、决定了什么
4. 再添加一条个人笔记：自己的想法和 action items
5. 之后在 Tab 4 笔记列表中搜索"Q3 规划"，能找到这两条笔记

---

## 六、数据模型

### 6.1 设计原则

**渐进式扩展**：每个版本只加必要的字段，不预设未来版本的字段。SwiftData 支持 migration，按需增长比预定义大模型更安全。

### 6.2 V1.0 模型

**PlanItem**（核心模型）：
```
id: UUID
title: String
notes: String (默认 "")
dueDate: Date? (可选，无日期 = "未安排")
status: ItemStatus (pending / completed)
priority: Priority (none / low / medium / high)
createdAt: Date
updatedAt: Date
completedAt: Date?
reminderTime: Date? (自定义提醒时间，nil 则使用全局默认)
```

**Note**（笔记）：
```
id: UUID
title: String
content: String (默认 "")
createdAt: Date
updatedAt: Date
```

V1.0 的 Note 是独立笔记，没有关联关系。两个模型各自独立。

**UserSettings**（用户设置，家族统一模式）：
```
id: UUID
userName: String
avatarImageData: Data?
defaultReminderTime: Date (默认 09:00)
appearance: Appearance (system / light / dark)
language: Language
```

**V1.0 共三个模型。**

### 6.3 V1.1 新增模型

**Collection**（收藏集）：
```
id: UUID
name: String
icon: String (SF Symbol name)
color: String (hex)
sortOrder: Int
createdAt: Date
```

**Tag**（标签）：
```
id: UUID
name: String
color: String (hex)
```

**PlanItem 新增字段**：
```
collection: Collection? (relationship)
tags: [Tag] (relationship)
parentItem: PlanItem? (子任务的父任务)
subItems: [PlanItem] (子任务)
sortOrder: Int
```

**Note 新增字段**：
```
tags: [Tag] (relationship，复用 Tag 模型)
```

### 6.4 V1.2 新增字段

**PlanItem 新增**：
```
type: ItemType (task / event)  — 默认 task
startDate: Date? (事件开始时间)
endDate: Date? (事件结束时间)
location: String? (事件地点)
isRecurring: Bool
recurrenceRule: RecurrenceRule? (relationship)
```

**Note 新增字段**：
```
linkedItem: PlanItem? (relationship，关联到事件或任务)
```

一条笔记可以关联到一个 PlanItem（事件或任务），也可以不关联（独立笔记）。

**RecurrenceRule**（重复规则）：
```
id: UUID
frequency: Frequency (daily / weekly / monthly / yearly / weekdays)
interval: Int (每 N 个周期)
endDate: Date? (重复结束日期)
```

### 6.5 V1.3 新增模型

**Idea**（想法，独立模型）：
```
id: UUID
title: String
content: String (默认 "")
createdAt: Date
status: IdeaStatus (active / converted / archived)
convertedItemId: UUID? (转化后关联的 PlanItem)
```

想法和任务/笔记的生命周期不同：想法是"可能要做的事"，核心动作是"转化"或"放弃"，不是"完成"或"记录"。

**Document**（证件，元数据模型）：
```
id: UUID
name: String (如 "我的护照")
type: DocumentType (idCard / passport / driverLicense / hkMacaoPass / socialSecurity / other)
photoFileNames: [String] (照片文件名列表，正面/反面/其他)
expiryDate: Date? (到期日期)
notes: String (备注)
createdAt: Date
updatedAt: Date
```

照片文件存储在 App 沙盒的 `Documents/Credentials/` 目录，SwiftData 只存文件名引用。

**ReviewEntry**（复盘记录）：
```
id: UUID
date: Date
type: ReviewType (daily / weekly)
notes: String
completedCount: Int
pendingCount: Int
aiSummary: String?
createdAt: Date
```

---

## 七、AI 模块（家族统一）

### 7.1 共享能力

与 1Cash/1Life/1Track 保持高度一致：

- 气泡对话 UI（用户右、AI 左，气泡旁服务商标识）
- 6 家 AI 服务商（Claude / ChatGPT / Kimi / 通义千问 / 豆包 / 腾讯混元）
- API Key 存 Keychain
- 消息跨会话持久化
- 快捷建议 Chips
- 语音输入

### 7.2 结构化意图（按版本）

**V1.0**：
- `create_task`：自然语言 → 单个任务草稿 → 确认页 → 保存

**V1.2 新增**：
- `create_event`：自然语言 → 事件草稿（含开始/结束时间）→ 确认页 → 保存
- `create_tasks`：一句话多任务 → 逐笔确认
- `get_today_tasks`：今日事项摘要注入 AI 上下文
- `get_upcoming`：近期事项注入 AI 上下文

**V1.3 新增**：
- `get_review_summary`：复盘统计数据注入上下文，AI 生成回顾摘要
- `organize_notes`：AI 建议笔记整理和分类
- `create_idea`：自然语言 → 想法草稿

### 7.3 AI 确认页（家族统一风格）

- 结构化卡片展示 AI 解析结果（标题、日期、优先级等）
- 用户可修改所有字段
- 确认后保存，提供撤销 toast
- 不确认时 AI 不写入数据库

---

## 八、通知系统

### 8.1 V1.0 基础通知

**触发规则**：任务设置了 dueDate → 自动在到期当天发本地通知。

**默认时间**：上午 9:00，可在设置中修改（"默认提醒时间"）。

**单任务自定义**：任务详情中可选择提醒时间覆盖默认值：
- 当天（使用默认时间）
- 提前 1 天
- 提前 1 小时
- 自定义时间

**通知内容**：
- 标题：「1Day」
- 正文：任务标题
- 快捷操作：通知上支持「标记完成」action（UNNotificationAction）

**取消逻辑**：任务完成或删除时，取消对应的 pending notification。

**权限请求**：首次设置任务日期时触发系统权限弹窗，或在 Onboarding 中引导。

**技术方案**：`UNUserNotificationCenter`，纯本地，不需要服务端。

### 8.2 V1.2 增强

- 事件开始前提醒（默认提前 15 分钟）
- 重复任务/事件的重复通知
- 每日晨间摘要通知（「今天有 3 件事要做」）

### 8.3 V1.3 增强

- 证件到期前 30 天提醒

---

## 九、Onboarding（家族统一骨架）

### 步骤

```
Welcome → Language → Profile → 默认提醒时间 → AI Config（可跳过）→ Ready
```

### 各步骤说明

1. **Welcome**：产品承诺——「安排要做的，记录在想的，复盘已完成的」+ 三个核心价值点
2. **Language**：语言选择
3. **Profile**：头像 + 昵称（家族统一 `UserAvatarView`）
4. **默认提醒时间**（域特定）：「每天几点提醒你待办事项？」预设选项 8:00 / 9:00 / 10:00 + 自定义
5. **AI Config**：服务商选择 + Key 输入 + 测试连接，可跳过
6. **Ready**：完成摘要 + 进入 App

---

## 十、交互设计

### 10.1 任务创建流程

**快速创建**（今日视图顶部输入栏）：
1. 用户输入文字 → 回车
2. 创建一个今天的任务（标题 = 输入内容，日期 = 今天，优先级 = none）
3. 任务出现在今日列表中，带入场动画
4. 触觉反馈（success）

**完整创建**（右上角 + 按钮 → Sheet）：
1. 标题（必填）
2. 备注（可选）
3. 日期（可选，日期选择器）
4. 优先级（可选，none/low/medium/high）
5. 提醒时间（可选，覆盖默认）
6. 收藏集（V1.1，可选）
7. 标签（V1.1，可选）

**AI 创建**（Tab 3 聊天）：
1. 用户自然语言输入，如「明天下午三点开会」
2. AI 解析 → 返回结构化草稿
3. 确认页展示：标题「开会」/ 日期「明天 15:00」
4. 用户可修改任何字段
5. 确认 → 保存 → 撤销 toast

### 10.2 笔记创建流程

**从 Tab 4 创建独立笔记**：
1. 点击「+」按钮 → 进入笔记编辑页
2. 输入标题（可选）+ 正文
3. 自动保存（每次退出编辑页时保存）
4. 无标题时以正文第一行作为列表预览

**从事件详情创建关联笔记**（V1.2）：
1. 在事件详情页底部点击「添加笔记」
2. 进入笔记编辑页（自动关联到该事件）
3. 编辑完成后，笔记同时出现在事件详情和 Tab 4 列表中

### 10.3 任务完成流程

1. 长按任务 → 「标记完成」（详情页按钮和通知横幅动作等效）
2. 划过横线动画（0.3s）
3. 触觉反馈（success）
4. 任务移至"已完成"区域（灰显）
5. 如有对应通知 → 自动取消

### 10.4 证件使用流程（V1.3）

**典型场景：用户需要上传身份证照片**
1. 打开 1Day → 设置 → 证件管理
2. Face ID 验证
3. 点击「身份证」卡片 → 查看正反面大图
4. 长按照片 → 「保存到相册」或「分享」
5. 切换到需要上传的 App → 从相册选取刚保存的照片

### 10.5 设计语言（家族统一）

- 白色主调，数字和文字是主角
- 卡片式布局（圆角 16-20pt，`secondarySystemBackground` 背景）
- SF Rounded 字体
- 克制用色，颜色是意义的载体不是装饰
- 留白是呼吸不是空洞
- 成功操作提供触觉反馈（Haptic）
- 破坏性操作需二次确认

### 10.6 动效

| 场景 | 动效方式 | 时长 |
|------|---------|------|
| 完成任务 | 划过横线 + 移至已完成区 | 0.3s |
| 添加任务 | 从底部滑入列表 | 0.3s |
| 删除任务/笔记 | 行直接消失，撤销条从底部滑入（5 秒） | 0.25s |
| 保存成功 | 触觉反馈 | — |

---

## 十一、技术架构

### 11.1 技术栈（家族统一）

- **框架**：SwiftUI (iOS 17+)
- **持久化**：SwiftData（从 V1.0 开始）
- **文件存储**：证件照片存 App 沙盒 `Documents/Credentials/`
- **AI**：家族统一 AI 服务层（6 家服务商、Keychain 存储、Prompt JSON 结构化解析）
- **通知**：UNUserNotificationCenter（本地通知）
- **安全**：LocalAuthentication（Face ID / Touch ID，用于证件管理）
- **认证**：无需登录，无账号系统

### 11.2 目录结构（家族统一）

```
1Day/
├── App/
├── Models/
├── Services/
├── Services/AI/
├── ViewModels/
├── Components/
├── Views/
│   ├── AIChat/
│   ├── Notes/
│   ├── Onboarding/
│   └── Settings/
└── Assets.xcassets/
```

### 11.3 服务层边界

V1.0 起，View 只负责展示、输入和导航；涉及数据写入、状态变更、副作用的业务动作应收口到 Services。

**当前服务边界：**
- `PlanItemService`：任务创建、完成/撤销完成、删除；后续接入通知时，任务相关通知取消/重排也从这里触发。
- `NotificationService`（V1.0 通知阶段）：本地通知权限、任务提醒调度、取消 pending notification、通知快捷操作。
- `JSONBackupService`（V1.0 备份阶段）：导出/导入任务、笔记、设置，不包含 API Key 和证件照片。
- `AIIntentService` / AI 业务层：自然语言解析为任务草稿，不直接写入 SwiftData。

**结构约束：**
- View 不直接承担跨页面复用的业务规则。
- View 可以读取 SwiftData 查询结果，但写入、删除、完成状态切换优先通过服务层。
- AI、通知、备份这类副作用不放进 View，必须通过服务层进入。
- V1.2 前不为 `PlanItem` 预留事件字段；事件字段按版本迁移加入。

### 11.4 文档权威关系

为避免 PRD、范围文档、数据模型和交互流程互相打架，1Day 的文档按以下优先级执行：

1. `PRD.md`：产品定位、版本路线、功能边界的最高来源。
2. `docs/MVP_SCOPE.md`：当前版本开发 checklist；判断“现在做不做”时以此为准。
3. `docs/DATA_MODEL.md`：字段名称、类型、默认值、版本迁移的唯一来源。
4. `docs/UX_FLOW.md`：页面流程、按钮行为、状态变化、错误处理的唯一来源。
5. `docs/AI_BEHAVIOR_SPEC.md`：AI 意图、草稿确认、上下文读取和安全边界的唯一来源。
6. `docs/REUSE_PLAN.md`：从家族 App 复用文件和改写顺序的工程参考。

当文档冲突时，先按以上顺序判断；若仍无法确定，更新上游文档后再实现代码。

---

## 十二、数据与隐私

### 12.1 数据存储
- 所有事项和笔记数据存储在本机 SwiftData 中。
- 证件照片存储在 App 沙盒内，不上传到任何服务器。
- iCloud 同步为远期规划，V1.0 不接入。

### 12.2 证件安全
- 进入证件管理页需 Face ID / Touch ID 验证。
- 证件照片不包含在 JSON 备份中（防止敏感数据泄露）。
- 未来 iCloud 同步时证件照片需单独加密处理。

### 12.3 AI 数据
- AI API Key 存在 iOS Keychain。
- AI 请求发送用户输入和必要上下文（今日事项）给所选 AI 服务商。
- AI 不直接写入数据库，生成草稿需用户确认。
- 笔记内容不会自动发送给 AI，仅在用户主动请求时（如复盘）发送摘要。

### 12.4 导出
- 支持 JSON 完整备份（事项、笔记、设置）。
- V1.1 起包含收藏集、标签。
- V1.3 起包含想法、复盘记录、证件元数据（不含照片）。
- 备份不包含 AI API Key 和证件照片。

---

## 十三、不做清单

- ❌ 不做记账（1Cash 的事）
- ❌ 不做饮食/营养记录（1Life 的事）
- ❌ 不做在线订阅追踪（1Track 的事）
- ❌ 不做即时通讯/社交
- ❌ 不做番茄钟/专注计时
- ❌ 不做看板/甘特图
- ❌ 不做账号系统/登录
- ❌ 不做 Web/后端（纯本地 App）
- ❌ 不做 Apple Calendar / Google Calendar 同步（远期评估）
- ❌ 不做协作/多人/共享清单
- ❌ 不做富文本/Markdown 编辑（笔记保持纯文本）

---

## 十四、验收标准

### V1.0 发布标准

**核心功能**：
- [ ] 任务 CRUD：创建、编辑、删除、标记完成
- [ ] 今日视图：展示今日待办、过期置顶、已完成折叠、底部进度摘要卡片
- [ ] 快速输入：今日视图顶部输入栏，回车创建今日任务
- [ ] 计划列表：所有任务按日期分组，未安排任务置顶
- [ ] 笔记 CRUD：创建、编辑、删除独立笔记
- [ ] 笔记列表：按更新时间排序，支持基础搜索
- [ ] 本地通知：到期任务按设定时间提醒，支持「标记完成」快捷操作

**AI**：
- [ ] AI 聊天：家族统一 UI，支持 6 家服务商
- [ ] create_task 意图：自然语言 → 草稿 → 确认页 → 保存（含撤销）

**基础设施**：
- [ ] Onboarding：Welcome → Language → Profile → 提醒时间 → AI Config（可跳过）→ Ready
- [ ] 设置：资料编辑、基础设置、AI 配置、数据导出、隐私说明
- [ ] JSON 备份/恢复（任务 + 笔记 + 设置）
- [ ] 空状态：每个 Tab 的空状态引导
- [ ] 本地化：简体中文 + English
